import { useState, useEffect } from 'react';import { View, Text, FlatList, TouchableOpacity, StyleSheet, ActivityIndicator } from 'react-native';import { useRouter } from 'expo-router';import { supabase } from '../../lib/supabase';import { COLORS } from '../../lib/constants';import { TDivider } from '../../components/Terminal';export default function CertificateList() {  const router = useRouter();  const [courses, setCourses] = useState<any[]>([]);  const [loading, setLoading] = useState(true);  useEffect(() => { loadCompleted(); }, []);  async function loadCompleted() {    const { data: { user } } = await supabase.auth.getUser();    if (!user) { setLoading(false); return; }    const { data: progress } = await supabase.from('user_progress').select('lesson_id, modules!inner(course_id)').eq('user_id', user.id).eq('completed', true);    if (!progress) { setLoading(false); return; }    const courseIds = [...new Set(progress.map((p: any) => p.modules?.course_id).filter(Boolean))];    if (courseIds.length === 0) { setLoading(false); return; }    const { data: coursesData } = await supabase.from('courses').select('*').in('id', courseIds);    if (coursesData) {      const completedCourses = [];      for (const course of coursesData) {        const { data: modules } = await supabase.from('modules').select('id, lessons(id)').eq('course_id', course.id);        if (modules) {          const totalLessons = modules.reduce((s: number, m: any) => s + (m.lessons?.length || 0), 0);          const completedLessons = progress.filter((p: any) => {            return modules.some((m: any) => m.lessons?.some((l: any) => l.id === p.lesson_id));          }).length;          if (completedLessons >= totalLessons) {            completedCourses.push(course);          }        }      }      setCourses(completedCourses);    }    setLoading(false);  }  if (loading) {    return (      <View style={styles.loading}>        <ActivityIndicator size="small" color={COLORS.terminal} />      </View>    );  }  return (    <View style={styles.container}>      <TouchableOpacity onPress={() => router.back()} style={styles.back}>        <Text style={styles.backText}>{'<'} back</Text>      </TouchableOpacity>      <Text style={styles.title}>{'>'} certificates</Text>      <Text style={styles.sub}>completed courses</Text>      <TDivider />      {courses.length === 0 ? (        <View style={styles.empty}>          <Text style={styles.emptyIcon}>[..]</Text>          <Text style={styles.emptyText}>complete a course to earn a certificate</Text>        </View>      ) : (        <FlatList          data={courses}          keyExtractor={(item: any) => item.id}          renderItem={({ item }: { item: any }) => (            <TouchableOpacity style={styles.card} onPress={() => router.push(`/certificate/${item.id}`)}>              <Text style={styles.cardIcon}>[*]</Text>              <View style={{ flex: 1 }}>                <Text style={styles.cardTitle}>{item.title}</Text>                <Text style={styles.cardLang}>[{item.language}]</Text>              </View>              <Text style={styles.cardArrow}>{'>'}</Text>            </TouchableOpacity>          )}        />      )}    </View>  );}const styles = StyleSheet.create({  container: { flex: 1, backgroundColor: COLORS.background, paddingHorizontal: 16 },  loading: { flex: 1, backgroundColor: COLORS.background, justifyContent: 'center', alignItems: 'center' },  back: { paddingTop: 60, marginBottom: 8 },  backText: { color: COLORS.textSecondary, fontSize: 13, fontFamily: 'monospace' },  title: { fontSize: 22, fontWeight: '700', color: COLORS.text, fontFamily: 'monospace' },  sub: { fontSize: 12, color: COLORS.textSecondary, fontFamily: 'monospace', marginTop: 4, marginBottom: 8 },  empty: { alignItems: 'center', paddingTop: 60 },  emptyIcon: { fontSize: 28, color: COLORS.textMuted, fontFamily: 'monospace', marginBottom: 12 },  emptyText: { color: COLORS.textMuted, fontSize: 13, fontFamily: 'monospace' },  card: {    flexDirection: 'row', alignItems: 'center', borderWidth: 1, borderColor: COLORS.border,    padding: 14, marginBottom: 8,  },  cardIcon: { color: COLORS.terminal, fontSize: 14, fontFamily: 'monospace', marginRight: 12 },  cardTitle: { fontSize: 14, fontWeight: '600', color: COLORS.text, fontFamily: 'monospace' },  cardLang: { fontSize: 11, color: COLORS.textMuted, fontFamily: 'monospace', marginTop: 4 },  cardArrow: { color: COLORS.textMuted, fontSize: 13, fontFamily: 'monospace' },});