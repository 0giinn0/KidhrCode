import { useState, useEffect } from 'react'
import { View, Text, FlatList, TouchableOpacity, StyleSheet, ActivityIndicator } from 'react-native'
import { supabase } from '../../lib/supabase'
import { COLORS } from '../../lib/constants'
import { TDivider } from '../../components/Terminal'
const PERIODS = ['daily', 'weekly', 'monthly', 'all_time']
export default function Leaderboard() {  const [leaders, setLeaders] = useState<any[]>([])
  const [period, setPeriod] = useState('all_time')
  const [loading, setLoading] = useState(true)
  useEffect(() => {    setLoading(true);    supabase.from('leaderboard').select('*').eq('period', period).order('xp', { ascending: false }).limit(50)      .then(({ data }) => {        if (data) setLeaders(data);        setLoading(false);      });  }, [period])
  return (    <View style={styles.container}>      <Text style={styles.title}>{'>'} leaderboard</Text>      <Text style={styles.sub}>who codes the most</Text>      <View style={styles.periodRow}>        {PERIODS.map(p => (          <TouchableOpacity            key={p}            style={[styles.periodBtn, period === p && styles.periodActive]}            onPress={() => setPeriod(p)}          >            <Text style={[styles.periodText, period === p && styles.periodTextActive]}>              {p.replace('_', ' ')}            </Text>          </TouchableOpacity>        ))}      </View>      <TDivider />      {loading ? (        <ActivityIndicator size="small" color={COLORS.terminal} style={{ marginTop: 40 }} />      ) : (        <FlatList          data={leaders}          keyExtractor={(item: any, i: number) => `${item.user_id}-${i}`}          ListEmptyComponent={            <View style={styles.empty}>              <Text style={styles.emptyIcon}>[..]</Text>              <Text style={styles.emptyText}>no rankings yet</Text>            </View>          }          renderItem={({ item, index }: { item: any; index: number }) => {            const rank = index === 0 ? '[##]' : index === 1 ? '[##]' : index === 2 ? '[##]' : `[${index + 1}]`;            return (              <View style={[styles.row, index < 3 && styles.topRow]}>                <Text style={styles.rank}>{rank}</Text>                <View style={styles.avatar}>                  <Text style={styles.avatarText}>@</Text>                </View>                <View style={styles.info}>                  <Text style={styles.name}>{item.username || 'anonymous'}</Text>                  <Text style={styles.level}>lv.{item.level || 1}</Text>                </View>                <Text style={styles.xp}>{item.xp || 0} xp</Text>              </View>            );          }}          contentContainerStyle={styles.list}        />      )}    </View>  );}const styles = StyleSheet.create({  container: { flex: 1, backgroundColor: COLORS.background, paddingTop: 60 },  title: { fontSize: 22, fontWeight: '700', color: COLORS.text, fontFamily: 'monospace', paddingHorizontal: 16 },  sub: { fontSize: 11, color: COLORS.textMuted, fontFamily: 'monospace', paddingHorizontal: 16, marginTop: 4, marginBottom: 16 },  periodRow: { flexDirection: 'row', marginHorizontal: 16, borderWidth: 1, borderColor: COLORS.border },  periodBtn: { flex: 1, paddingVertical: 10, alignItems: 'center' },  periodActive: { backgroundColor: COLORS.surfaceLight },  periodText: { color: COLORS.textMuted, fontSize: 10, fontFamily: 'monospace', letterSpacing: 1, textTransform: 'capitalize' },  periodTextActive: { color: COLORS.text },  list: { paddingHorizontal: 16, paddingBottom: 100 },  empty: { alignItems: 'center', paddingTop: 60 },  emptyIcon: { fontSize: 24, color: COLORS.textMuted, fontFamily: 'monospace' },  emptyText: { fontSize: 14, color: COLORS.textSecondary, fontFamily: 'monospace' },  row: {    flexDirection: 'row', alignItems: 'center', borderWidth: 1, borderColor: COLORS.border,    padding: 12, marginBottom: 6,  },  topRow: { borderColor: COLORS.primary },  rank: { color: COLORS.textSecondary, fontSize: 11, fontFamily: 'monospace', fontWeight: '700', width: 40 },  avatar: {    width: 32, height: 32, borderWidth: 1, borderColor: COLORS.border,    justifyContent: 'center', alignItems: 'center', marginRight: 10,  },  avatarText: { color: COLORS.textMuted, fontSize: 12, fontFamily: 'monospace' },  info: { flex: 1 },  name: { color: COLORS.text, fontSize: 13, fontFamily: 'monospace', fontWeight: '600' },  level: { color: COLORS.textMuted, fontSize: 10, fontFamily: 'monospace', marginTop: 2 },  xp: { color: COLORS.terminal, fontSize: 12, fontFamily: 'monospace', fontWeight: '700' },});
